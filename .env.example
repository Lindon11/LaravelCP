APP_DEBUG=0

# Database
DB_HOST=db
DB_DATABASE=gangster_legends
DB_USER=dev
DB_PASS=dev

# Mail (optional; if app supports SMTP config via env later)
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=
